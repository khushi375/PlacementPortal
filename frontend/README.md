# Frontend

Student portal and Placement Center UI for the Nexora Placement Portal.

See the repository root `README.md` for setup, environment variables, and build/test commands.

Do not add Gemini or other server secrets here. Frontend env vars must not include `AI_API_KEY`.
